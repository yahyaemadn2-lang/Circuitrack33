export async function GET(req: Request) {
  return Response.json({ message: "GET companies placeholder" });
}

export async function POST(req: Request) {
  return Response.json({ message: "POST companies placeholder" });
}

export async function PATCH(req: Request) {
  return Response.json({ message: "PATCH companies placeholder" });
}

export async function DELETE(req: Request) {
  return Response.json({ message: "DELETE companies placeholder" });
}