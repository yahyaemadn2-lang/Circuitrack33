export async function GET(req: Request) {
  return Response.json({ message: "GET offers placeholder" });
}

export async function POST(req: Request) {
  return Response.json({ message: "POST offers placeholder" });
}

export async function PATCH(req: Request) {
  return Response.json({ message: "PATCH offers placeholder" });
}

export async function DELETE(req: Request) {
  return Response.json({ message: "DELETE offers placeholder" });
}
