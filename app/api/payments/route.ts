export async function GET(req: Request) {
  return Response.json({ message: "GET payments placeholder" });
}

export async function POST(req: Request) {
  return Response.json({ message: "POST payments placeholder" });
}
