export async function GET(req: Request) {
  return Response.json({ message: "GET admin placeholder" });
}

export async function POST(req: Request) {
  return Response.json({ message: "POST admin placeholder" });
}

export async function PATCH(req: Request) {
  return Response.json({ message: "PATCH admin placeholder" });
}

export async function DELETE(req: Request) {
  return Response.json({ message: "DELETE admin placeholder" });
}
